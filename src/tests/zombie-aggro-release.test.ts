import{describe,expect,it}from"vitest";import{createZombieMind,updateZombieMind,ZOMBIE_AGGRO_RELEASE_PROFILE}from"../systems/zombie-ai-system";
describe("zombie aggro release",()=>{it.each(["walker","runner"] as const)("releases %s after its grace and uses reengage hysteresis",(kind)=>{const profile=ZOMBIE_AGGRO_RELEASE_PROFILE[kind];let mind=updateZombieMind(createZombieMind(),{canSeeTarget:true,targetPosition:{x:0,y:0},targetId:"player",nowMs:0});mind=updateZombieMind(mind,{canSeeTarget:false,targetAlive:true,targetDistanceSquared:(profile.releaseDistance+1)**2,zombieKind:kind,zombiePosition:{x:10,y:10},nowMs:100});expect(mind.visualLock).toBe(true);mind=updateZombieMind(mind,{canSeeTarget:false,targetAlive:true,targetDistanceSquared:(profile.reengageDistance+20)**2,zombieKind:kind,zombiePosition:{x:10,y:10},nowMs:100+profile.graceMs+1});expect(mind.visualLock).toBe(true);mind=updateZombieMind(mind,{canSeeTarget:false,targetAlive:true,targetDistanceSquared:(profile.releaseDistance+1)**2,zombieKind:kind,zombiePosition:{x:10,y:10},nowMs:100+profile.graceMs+2});expect(mind.visualLock).toBe(false);expect(mind.currentTargetId).toBeUndefined();expect(mind.lastSeenTargetPosition).toEqual({x:10,y:10});});});
