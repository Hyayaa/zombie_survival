import{describe,expect,it}from"vitest";import{createCityBlockMap}from"../data/map-definitions";import{validateMap}from"../data/map-validation";
describe("road-first map validation",()=>{it("keeps the generated v6 world reachable and deterministic",()=>{const first=createCityBlockMap(418),second=createCityBlockMap(418),result=validateMap(first);expect(result.errors,result.errors.join("\n")).toEqual([]);expect(first.mapId).toBe("multi-city-v3-road-first");expect(first.mapGenerationVersion).toBe(6);expect(first.roadGraph).toEqual(second.roadGraph);expect(first.buildingLots).toEqual(second.buildingLots);expect(first.districtProps).toEqual(second.districtProps);});});
